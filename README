Test NodeJS project.
Test requirements: postgres.js, sessions, API calls, redis caching. User balance in requirements was of type FLOAT.

1. docker-compose up -d
2. npm run migrate
3. POST http://localhost:3000/api/auth/login (username: test, password: password)

Example .env in .env.example
